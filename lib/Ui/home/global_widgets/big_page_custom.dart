import 'package:flutter/material.dart';

class BigPageCustom extends StatelessWidget {
  const BigPageCustom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
