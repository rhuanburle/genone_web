import 'package:flutter/material.dart';

class SmallPageCustom extends StatelessWidget {
  const SmallPageCustom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
