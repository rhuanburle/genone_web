import 'package:flutter/material.dart';

class GlobalVision {
  int id;
  String title;
  String text;
  Color colorButton = Colors.grey;

  GlobalVision({required this.id, required this.title, required this.text});
}